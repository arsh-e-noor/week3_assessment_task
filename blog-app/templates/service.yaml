apiVersion: v1
kind: Service

metadata:
  name: {{ include "blog-app.fullname" . }}

spec:
  type: {{ .Values.service.type }}

  selector:
    app: blog-app

  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
      nodePort: 30090